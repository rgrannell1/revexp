{
  "name": "revexp",
  "type": "module",
  "version": "1.0.0",
  "description": "Reverse regular-expression generator.",
  "types": "dist/src/revexp.d.ts",
  "main": "dist/src/revexp.js",
  "author": "Róisín Grannell <r.grannell2@gmail.com>",
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "test": "npm run tsc && npm run clean:declarations && npx tap dist/test --no-esm --no-coverage --timeout 300",
    "clean:declarations": "find dist/test -type f -name '*.d.ts' -delete",
    "standard": "npx standard --fix"
  },
  "dependencies": {
    "@types/diff": "^4.0.2",
    "@types/tap": "^14.10.1",
    "diff": "4.0.2",
    "randexp": "^0.5.3"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chalk": "^4.1.0",
    "standard": "^14.3.4",
    "tap": "^14.10.7",
    "typescript": "^3.9.7"
  },
  "nyc": {
    "exclude": [
      "build/**",
      "pulpfile.js",
      "**/tests"
    ]
  }
}
